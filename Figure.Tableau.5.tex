\begin{tikzpicture}[grow=up,
execute at begin node=$, execute at end node=$,
every node/.style={opacity=1},
every child/.style={edge from parent/.style={opacity=0}}]
\def\dist{0.4cm}
\node(e) {\PModelsf \falses} [sibling distance=2*\dist+3cm] 
	child {node(1) {\PModelsf D(d)} [sibling distance=\dist+3cm]
		child[xshift=2cm] {node(11) {\PModelsf P(d,a_0)}}
		child[xshift=2cm] {node(12) {\PModelsf P(d,a_0)\to D(d)}
			child {node(121) {\PModelsf\forall\alpha\beta(P(\alpha,\beta)\to D(\alpha))}}}}
	child {node(2) {\PModelsf D(d)\to\falses}
		child {node(21) {D(d)\PModelsf\falses}
			child {node[label={[yshift=-0.3cm]above:\vdots}](211) {}}}};



%set of all positions in the tree
\def\nodes{,1,2,11,12,21,121,211}
\def\identifier{5}
\input{Draw.Tableau}
\end{tikzpicture}